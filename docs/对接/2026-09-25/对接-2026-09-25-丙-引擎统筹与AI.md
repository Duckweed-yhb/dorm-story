# 与成员丙对接（引擎统筹与 AI 主程）

> 日期：2026-09-25 · 阶段1 Day1 · 成员乙（闭环与协作）发起
> 用途：今天对齐会用的 `addExp` 对接口径清单，逐项打勾，会后更新结论。

## 一、我（乙）需要丙提供

| 项 | 说明 | 丙确认 |
| --- | --- | --- |
| `addExp` 云函数 | 入参 `{dormId, action}`，由丙负责计算并写流水 | ⬜ |
| action 枚举清单 | 明确 `value_duty`(+10)、`full_checkin`(+30) 等命名与数值 | ⬜ |
| 返回格式 | `addExp` 的返回（如 `{expAdded, level, attrs}`）供前端回显 | ⬜ |
| `aiLog` 集合 | 阶段3 用，阶段1 先确认命名规范即可 | ⬜ |

## 二、我（乙）提供给丙

| 项 | 说明 | 丙确认 |
| --- | --- | --- |
| 调用时机 | `completeDuty` 完成写库后 → `addExp({dormId, action:'value_duty'})` | ⬜ |
| 调用时机 | `doCheckin` 判定当天全员打卡后 → `addExp({dormId, action:'full_checkin'})` | ⬜ |
| 数据源 | 阶段3 AI 排班/活跃度/周报需读 `duty` / `checkin` 数据 | ⬜ |

## 三、对齐点（今天会上逐项核对）

| 对齐项 | 丙口径 | 乙口径 | 结论 |
| --- | --- | --- | --- |
| `addExp` 入参 | 待定 | `{dormId, action}` | ⬜ |
| `addExp` 返回 | 待定 | `{expAdded, level, attrs}` | ⬜ |
| action 命名 | 待定 | `value_duty` / `full_checkin` | ⬜ |
| 经验值数值 | 丙定 | 值日 +10、全员打卡 +30 | ⬜ |
| 失败兜底 | 丙定 | `addExp` 失败不影响值日/打卡主流程 | ⬜ |

## 四、关键约定（今天务必定死）

- [ ] **action 枚举以丙的清单为准**，乙只照用，不自己发明；
- [ ] **经验值计算只允许发生在 `addExp`**，乙端不写任何 XP 数值；
- [ ] `addExp` 失败时乙的主流程（值日/打卡入库）不能中断，仅记日志。

---

> 结论区（会后填）：______
