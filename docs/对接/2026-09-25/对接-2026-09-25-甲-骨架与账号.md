# 与成员甲对接（骨架与账号主程）

> 日期：2026-09-25 · 阶段1 Day1 · 成员乙（闭环与协作）发起
> 用途：今天对齐会用的接口/集合口径清单，逐项打勾，会后更新结论。

## 一、我（乙）需要甲提供

| 项 | 说明 | 甲确认 |
| --- | --- | --- |
| 登录态 | 小程序端能取到当前 `openid`、`dormId`、`memberId` | ⬜ |
| `dorm` / `member` 集合 | 已建好并写入宿舍/成员数据，权限可读 | ⬜ |
| `getMembers(dormId)` | 返回成员名单（排班要用，需含 `memberId`、加入顺序） | ⬜ |
| `getDorm(dormId)` | 返回宿舍信息（含 `lastAssignIndex` 字段占位） | ⬜ |
| 底部导航 / 公共组件 | 值日、打卡页有可用的 tab 与空态/加载态组件 | ⬜ |

## 二、我（乙）提供给甲

| 项 | 说明 | 甲确认 |
| --- | --- | --- |
| `scheduleDuty` / `getDuty` | 接口签名：`{dormId, date?}` → `{dutyList}` | ⬜ |
| `doCheckin` / `getCheckin` | 接口签名：`{dormId, memberId, date?}` | ⬜ |
| 首页面板占位 | 首页需展示值日状态、打卡入口，留好数据接口位 | ⬜ |

## 三、对齐点（今天会上逐项核对）

| 对齐项 | 甲口径 | 乙口径 | 结论 |
| --- | --- | --- | --- |
| `dormId` 字段名 | 待定 | `dormId` | ⬜ |
| `member` 角色字段 | 待定 | `role`(宿舍长/成员) | ⬜ |
| 登录态传递 | 云函数用 `openid` 上下文 | 云函数内取 openid，前端传 `dormId` | ⬜ |
| `dorm.lastAssignIndex` | 需在 `dorm` 集合加该字段 | 排班公平性依赖 | ⬜ |
| 接口命名 | 待定 | 上表签名 | ⬜ |

## 四、阻塞项（需甲今天确认）

- [ ] AppID 已拿到？
- [ ] 云开发环境已开通并 `init` 成功？
- [ ] 工程能否跑起、进入云环境？

---

> 结论区（会后填）：______
